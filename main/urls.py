from django.urls import path
# from .views import setupView
# from .views import (
#     Home,
# )


url_patterens = [
    # TODO: the setup route here are for first time web app setup wizard.
    # path('setup/', setupView, name="setup"),
    # path('', Home, name="home"),

    # path('projects/', Projects, name="projects"),
    # path('project/<int:id>', Project, name="project"),
    # path('project/<int:id>/delete/', DeleteProject, name="delete_project"),
    # path('project/<int:id>/update/', UpdateProject, name="update_project"),
    # path('project/create', CreateProject, name="create_project"),

    # path('account-types/', AccountTypes, name="account_types"),
    # path('account-type/<int:id>', AccountType, name="account_type"),
    # path('account-type/<int:id>/delete/', DeleteAccountType, name="delete_account_type"),
    # path('account-type/<int:id>/update/', UpdateAccountType, name="update_account_type"),
    # path('account-type/create', CreateAccountType, name="create_account_type"),

    # path('accounting-entries/', AccountingEntries, name="accounting_entries"),
    # path('accounting-entry/<int:id>', AccountingEntry, name="accounting_entry"),
    # path('accounting-entry/<int:id>/delete/', DeleteAccountingEntry, name="delete_accounting_entry"),
    # path('accounting-entry/<int:id>/update/', UpdateAccountingEntry, name="update_accounting_entry"),
    # path('accounting-entry/create', CreateAccountingEntry, name="create_accounting_entry"),

    # path('labels/', Labels, name="labels"),
    # path('label/<int:id>', Label, name="label"),
    # path('label/<int:id>/delete/', DeleteLabel, name="delete_label"),
    # path('label/<int:id>/update/', UpdateLabel, name="update_label"),
    # path('label/create', CreateLabel, name="create_label"),

    # path('entry-items/', EntryItems, name="entry_items"),
    # path('entry-item/<int:id>', EntryItem, name="entry_item"),
    # path('entry-item/<int:id>/delete/', DeleteEntryItem, name="delete_entry_item"),
    # path('entry-item/<int:id>/update/', UpdateEntryItem, name="update_entry_item"),
    # path('entry-item/create', CreateEntryItem, name="create_entry_item"),
]